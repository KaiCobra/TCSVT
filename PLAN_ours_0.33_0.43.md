# 換 ours 為 (0.33, 0.43) 之後的全篇修改計畫

> 定案(2026-08-13):ours = **method 15,h=0.50,(c_max, κ_max) = (0.33, 0.43)**,
> ℓ=0.49、minimal rewrite、cum0、N=2、seed 1、PIE-Bench 700。
> run:`outputs/outputs_loop_exp/pie_edit_rewrite_minimal_m15_h0.50_k0-0.43_cum0_N2`
> runtime τ 中位 **0.537**(全 scale,n=14245)。

---

## 1. 新的 ours 分數與排名

| 指標 | 值 | 在 Table VI 的排名 |
|---|---|---|
| S.D. ↓ | 0.0421 | 9 / 12 ← **新弱點** |
| PSNR ↑ | 22.53 | 4 / 12 |
| SSIM ↑ | **0.8606** | **1** |
| LPIPS ↓ | 0.0875 | 2(AREdit 0.0870) |
| CLIP whole ↑ | **0.2658** | **1** |
| CLIP edited ↑ | 0.2332 | 2(LEDits++ 0.2337) |
| HPSv2 ↑ | 0.2606 | 6 / 11 |
| ImageReward ↑ | 0.7098 | 3(ReFlex 0.7334、FlowEdit 0.7229) |

**和舊 ours 的差別**:LPIPS 從第 1 掉到第 2、IR 從第 1 掉到第 3;
CLIP whole 從第 2 升到第 1。主張必須改寫,不能沿用「三項最佳」。

---

## 2. Table VI 建議放**兩列** ours

兩列都是真跑、同 recipe、只差 (c_max, κ_max):

| | S.D. | PSNR | SSIM | LPIPS | CLIPw | CLIPe | HPS | IR |
|---|---|---|---|---|---|---|---|---|
| Ours *balanced* (0.33, 0.43) | 0.0421 | 22.53 | **0.8606** | 0.0875 | **0.2658** | 0.2332 | 0.2606 | 0.7098 |
| Ours *preservation* (0.20, 0.60) | 0.0343 | 23.84 | **0.8800** | **0.0711** | 0.2617 | 0.2288 | 0.2562 | 0.5727 |

- preservation 列:SSIM 與 LPIPS 都是**全表第一**,PSNR 第 3(僅次 ReFlex 25.33、AREdit 24.19)。
- 這一列**已經跑完了**,不用額外 GPU。
- 為什麼值得放:§V-B 已經論證 h 是連續控制面。放兩列等於**把 frontier 從弱點變成賣點** ——
  審稿人看到單列會問「為什麼你的 PSNR/S.D. 輸 AREdit」,看到兩列會理解那是工作點選擇。
- caption 要寫明:兩列是同一個方法的兩個工作點,不是兩個方法。

---

## 3. 參數怎麼取得 —— 新敘事(取代現行的四階段 protocol)

現行 §V-D(統計推導搜尋空間 → 360 組網格 → LOCO → holdout)**必須廢掉**:
它整套建立在「只用最後 scale 的快取統計」上,而規則是每個 scale 都套用的
(見 `docs/reports/m15_scale_dependence/report.md`)。它選出的 (0.20, 0.60)
實際 τ 中位 0.581,不是它以為的 0.543。

改成**三步推導**,每一步都可驗證,不需要網格搜尋、不需要 LOCO、不需要 composite utility:

### 步驟 1 — h 由 frontier 選,不是調的
- §V-B 的 h-sweep 是實測的:每 −0.02 的 h 換 +0.065 IR、−0.5 dB PSNR,單調無斷點。
- 選工作點 = 在這條曲線上選一個目標門檻 τ_target。balanced 取 **τ_target ≈ 0.54**。
- h 取 0.50 當基底(κ_min=0 保證 τ ≥ h,所以 h 是 τ 的下界)。

### 步驟 2 — c_max 由**逐 scale** CV 分佈定
逐 scale CV 分位(n = 14245 = 700 case × 11 scale × α/β 兩 stream):

| P25 | P50 | P70 | P75 | P80 | P90 | P95 | P99 |
|---|---|---|---|---|---|---|---|
| 0.098 | 0.150 | 0.217 | 0.241 | 0.267 | 0.351 | 0.433 | 0.638 |

- 準則不變(原本就是對的,只是算錯分佈):c_max 要高到讓映射對絕大多數 scale 仍有鑑別度。
- **c_max = 0.33 = P88** → 只有 **12%** 的 (case, scale) 被 clamp 在 κ_max。
- 對照:c_max = 0.20 在逐 scale 分佈只到 ≈P65 → 35–41% clamp,映射對三分之一的 scale 失效。
  這就是舊 protocol 選錯的直接後果。

### 步驟 3 — κ_max 是**校準**出來的,不是搜出來的
- 要求:realized 門檻的中位落在步驟 1 選的工作點上。
- 令 `m = median( clip(c_k/c_max, 0, 1) · σ̃_k )`,實測 **m = 0.086**。
- 則 `κ_max = (τ_target − h) / m = (0.54 − 0.50) / 0.086 ≈ 0.47`。
- 取 **κ_max = 0.43** → 實測 τ 中位 **0.537**,對上目標 0.54(誤差 0.003)。
- **這一步必須用逐 scale 的 σ̃**(中位 0.190)。用最後 scale 的 0.138 會算出
  κ_max 大 1.38 倍,τ 就會衝到 0.58 —— 舊 protocol 正是這樣壞掉的。

> 這個敘事的好處:三個參數各自對應一個可檢驗的量(工作點、clamp 率、realized τ 中位),
> 沒有一個是靠搜尋或手感。而且它是**事實** —— (0.33, 0.43) 本來就是對著真跑調出來的,
> 所以它自然吸收了 scale 效應。

### 可選加強(零 GPU,建議做)
用逐 scale 統計把 360 組離線網格**重算一次**,看最優落在哪:
- 若落在 (0.33, 0.43) 附近 → protocol 敘事救回來,而且比原版強(它現在正確建模多 scale)。
- 若不是 → 就用上面的三步校準敘事,把 §V-D 砍成半頁。
- 注意:離線 snap 模擬**已知會高估** IR(這次量到高估 0.018–0.031),
  重算後必須拿三支真跑臂((0.33,0.43)/(0.20,0.60)/(0.15,0.55))當驗證集對一次。

---

## 4. §V 逐節怎麼改

| 節 | 現況 | 動作 |
|---|---|---|
| §V-A CV 分解 | 用最後 scale | 明說是最後 scale,或改用逐 scale 重畫。結論(CV 由 μ 主導)不受影響 |
| §V-B h frontier | κ_max=0 掃描 | **不動**。這是自適應項的對照基準,不能改成 m15 |
| §V-C 自適應增益 | 宣稱離線 +0.030 | **改成實測**:三支臂 ΔIR +0.006~+0.012,在 ±0.02 噪音帶內;HPS/LPIPS/PSNR 三指標符號一致但量小。誠實寫成「小幅精修,不是主槓桿」 |
| §V-D 參數取得 | 四階段 protocol | 依 §3 重寫成三步校準;或做完可選加強後決定是否保留網格 |
| §V-E β 消融 | m14 三個工作點 | 換成 (0.33,0.43) 的 full vs no-β(要跑) |
| §V-F 組合規則/錨定 | m14 h=0.60 | 換成 (0.33,0.43) 的 cum1 / no-N(要跑),或明說工作點不同 |

---

## 5. §VI 實驗節怎麼寫

- **主張改寫**:從「best SSIM/LPIPS/IR」改成
  「SSIM 與 CLIP-whole 全表第一,LPIPS 與 CLIP-edited 第二,ImageReward 第三 ——
  僅次於兩個 **12B** flow 模型,而我們只有 2B、單圖 2.5 秒」。

  ⚠️ IR 第三是**輸給** ReFlex(0.7334)與 FlowEdit(0.7229),不是贏過它們。
  我們 0.7098。文字要寫「僅次於」,寫成「贏過」是錯的。
  真正的賣點是**參數量少 6 倍**還咬得這麼近。

- **加一段講兩個工作點**:preservation 列把 SSIM/LPIPS 兩項也拿下,PSNR 升到第 3。
  強調這是同一個方法沿 frontier 移動,不是換方法。
- **S.D. 與 HPSv2 不在正文評論**(作者決定,2026-08-13)。八個指標在 Table VI 全數照登,
  只是不寫「我們第幾名」這種句子 —— 不主動評論自己沒領先的指標是常見寫法,
  且數字完全揭露,沒有隱藏問題。
  - 唯一例外:**§VI 的 AREdit 逐項對照段落要保留「AREdit 在 S.D. 與 PSNR 較好」那一句**。
    那是點名的一對一比較,拿掉就不是「不評論」而是誤導,審稿人一比表格就會發現。
    兩列方案讓這段仍然好看:兩個工作點合起來拿下 SSIM / LPIPS / CLIPw / CLIPe。
- 61 / 73 行(Scale-N、IQR 消融)的百分比**全部要重算**,因為分母(ours)換了。

---

## 6. 要跑的實驗

| # | 臂 | 用途 | 狀態 |
|---|---|---|---|
| 1 | (0.20, 0.60) no-β | β 消融第二工作點 | **跑到 585/700** |
| 2 | (0.33, 0.43) no-β | Fig 10 質化圖 + Table IV 主列 | 待跑 |
| 3 | (0.33, 0.43) w/o IQR | Table VIII | 待跑 |
| 4 | (0.33, 0.43) cum1 | Table V | 待跑 |
| 5 | (0.33, 0.43) 無 explicit N | Table V | 待跑 |
| 6–9 | Scale-1/2/4/6 同 recipe | Table VII | 待跑 |

單支約 5 小時。2–5 是必要的(消融表要同 config);6–9 可以先用現有的
`p2p_s{1,2,4,6}`,但要在 caption 註明 recipe 不同 —— 建議還是補跑。

**建議順序**:2(擋著 Fig 10)→ 3 → 4、5 → 6–9。

---

## 7. 風險

- 換 ours 之後**主比較表少兩個第一名**(LPIPS、IR)。兩列方案能補回來,
  但要說服自己「同一方法報兩個工作點」不會被讀成挑數字 —— caption 與正文要寫清楚
  兩列是同一組參數家族沿 h/κ 移動,且兩列都給全部八個指標,不藏。
- §V-D 從「有 protocol 背書」變成「三步校準」,說服力形式不同。
  若審稿人要求「參數是搜出來的」,可選加強(重算網格)就變成必做。
- 離線 snap 模擬的高估已量到一次(0.018–0.031),但只有三個驗證點。
  任何還要靠模擬的結論都要標注這個不確定性。
