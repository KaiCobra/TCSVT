\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{imgs/fig_besth_vs_cv.png}
    \caption{The per-case optimal threshold is not predictable from the attention distribution. Each point is one case; the vertical spread at every value of $c_k$ covers essentially the whole grid, and the binned median is nearly flat. A mapping from $c_k$ to a threshold can emit only one value per $c_k$, whereas the conditional distribution is almost independent of it --- this is the geometric reason why any prior mapping of this family is bounded by the best fixed threshold.}
    \label{fig:besth_vs_cv}
\end{figure}
